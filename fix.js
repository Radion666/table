const fs = require('fs'); 
let f = 'c:/Users/admin/Desktop/table/table/timesheet/src/main.ts'; 
c = c.replace('await app.listen(PORT, () =, 'await app.listen(PORT, \"0.0.0.0\", () =
let c = fs.readFileSync(f, 'utf8'); 
fs.writeFileSync(f, c); 
console.log('done'); 
